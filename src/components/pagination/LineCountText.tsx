export type PaginationLineCountTextProps = {
    children: string;
}
export default function LineCountText({children}: PaginationLineCountTextProps) {
    return (
        <>
            {children}
        </>
    );
}